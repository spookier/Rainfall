#include <stdlib.h>
#include <string.h>
#include <unistd.h>

int main(int argc, char **argv)
{
    char buffer[40];
    int count;

    (void)argc;
    count = atoi(argv[1]);

    if (count > 9)
        return 1;

    memcpy(buffer, argv[2], count * 4);

    if (count == 0x574f4c46)
        execl("/bin/sh", "sh", NULL);

    return 0;
}
